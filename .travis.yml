language: php
dist: trusty

php:
    - 5.3
    - 5.4
    - 5.5
    - 5.6
    - 7.0
    - 7.1
    - hhvm

before_script:
    - composer install --dev --prefer-dist --no-interaction

script:
    - ./vendor/bin/phpunit --coverage-text
